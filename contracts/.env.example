# Deployer private key — funded with testnet pathUSD on Tempo Moderato (pathUSD pays gas).
# Used by `forge script script/Deploy.s.sol` and by the E2E harness as the employer/funder.
PRIVATE_KEY=0xYourDeployerPrivateKeyHere
